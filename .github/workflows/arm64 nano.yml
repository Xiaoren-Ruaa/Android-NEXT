name: Build static nano for Android (arm64)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y make wget curl

      - name: Download Android NDK
        run: |
          wget https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
          unzip android-ndk-r25c-linux.zip
          echo "NDK_PATH=${PWD}/android-ndk-r25c" >> $GITHUB_ENV

      - name: Set up NDK environment
        run: |
          TOOLCHAIN=$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64
          TARGET=aarch64-linux-android
          API=21
          BIN_DIR=$TOOLCHAIN/bin
          echo "$BIN_DIR" >> $GITHUB_PATH
          echo "CC=$BIN_DIR/clang -target $TARGET$API" >> $GITHUB_ENV
          echo "CXX=$BIN_DIR/clang++ -target $TARGET$API" >> $GITHUB_ENV
          echo "AR=$BIN_DIR/llvm-ar" >> $GITHUB_ENV
          echo "STRIP=$BIN_DIR/llvm-strip" >> $GITHUB_ENV
          echo "TARGET=$TARGET" >> $GITHUB_ENV

      - name: Get latest ncurses version
        id: ncurses
        run: |
          LATEST=$(curl -s ftp://ftp.gnu.org/gnu/ncurses/ | grep -oP 'ncurses-\K[0-9]+\.[0-9]+' | sort -V | tail -n1)
          echo "version=${LATEST:-6.5}" >> $GITHUB_OUTPUT

      - name: Download and build ncurses (static)
        run: |
          wget ftp://ftp.gnu.org/gnu/ncurses/ncurses-${{ steps.ncurses.outputs.version }}.tar.gz
          tar xf ncurses-*.tar.gz
          cd ncurses-*
          ./configure --host=$TARGET --prefix=$PWD/install --without-shared --with-normal \
                      --without-debug --without-ada --enable-widec --enable-termcap \
                      CFLAGS="-fPIE"
          make -j$(nproc)
          make install.libs
          echo "NCURSES_DIR=$PWD/install" >> $GITHUB_ENV

      - name: Get latest nano version
        id: nano
        run: |
          LATEST=$(curl -s https://www.nano-editor.org/download.php | grep -oP 'nano-\K[0-9]+\.[0-9]+(?=\.tar\.xz)' | head -1)
          echo "version=${LATEST:-8.3}" >> $GITHUB_OUTPUT

      - name: Download and build nano (static)
        run: |
          wget https://www.nano-editor.org/dist/v${{ steps.nano.outputs.version }}/nano-${{ steps.nano.outputs.version }}.tar.xz
          tar xf nano-*.tar.xz
          cd nano-*
          export CPPFLAGS="-I$NCURSES_DIR/include -I$NCURSES_DIR/include/ncursesw"
          export LDFLAGS="-L$NCURSES_DIR/lib"
          ./configure --host=$TARGET --prefix=$PWD/install --with-ncursesw --enable-utf8
          make -j$(nproc) LDFLAGS="$LDFLAGS -static -pie" LIBS="-lncursesw"
          make install

      - name: Download prebuilt terminfo from Termux
        run: |
          # 下载 Termux 的 ncurses 包（包含 terminfo）
          wget https://packages.termux.org/apt/termux-main/pool/main/n/ncurses/ncurses_6.4-1_aarch64.deb
          ar x ncurses_*.deb
          tar xf data.tar.xz
          # terminfo 位于 ./data/data/com.termux/files/usr/share/terminfo
          mkdir -p terminfo
          cp -r ./data/data/com.termux/files/usr/share/terminfo/* terminfo/

      - name: Package final artifact
        run: |
          mkdir -p output
          cp nano-*/install/bin/nano output/
          cp -r terminfo output/
          cd output
          $STRIP nano
          tar -czf $GITHUB_WORKSPACE/nano-android-static.tar.gz *

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nano-android-static
          path: nano-android-static.tar.gz
